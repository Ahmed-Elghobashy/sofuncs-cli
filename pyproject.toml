[project]
name = "sofuncs"
version = "0.1.0"
description = "List native functions in Linux shared objects"
requires-python = ">=3.8"
readme = "README.md"
license = {text = "MIT"}

[project.scripts]
sofuncs = "sofuncs.__main__:main"

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["src"]

